local sheet = {
    -- top, bottom, left, right --
    {
        t = false,
        b = false,
        l = false,
        r = false,

        ctl = false,
        ctr = false,
        cbl = false,
        cbr = false,

        fill = false,
    }
}
return function(idx, x, y)
    
end